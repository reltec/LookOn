// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.WorkHistoryDataOnDemand;
import com.reltecsoft.lookon.domain.WorkHistoryIntegrationTest;
import com.reltecsoft.lookon.repository.WorkHistoryRepository;
import com.reltecsoft.lookon.service.WorkHistoryService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect WorkHistoryIntegrationTest_Roo_IntegrationTest {
    
    declare @type: WorkHistoryIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: WorkHistoryIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: WorkHistoryIntegrationTest: @Transactional;
    
    @Autowired
    WorkHistoryDataOnDemand WorkHistoryIntegrationTest.dod;
    
    @Autowired
    WorkHistoryService WorkHistoryIntegrationTest.workHistoryService;
    
    @Autowired
    WorkHistoryRepository WorkHistoryIntegrationTest.workHistoryRepository;
    
    @Test
    public void WorkHistoryIntegrationTest.testCountAllWorkHistorys() {
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", dod.getRandomWorkHistory());
        long count = workHistoryService.countAllWorkHistorys();
        Assert.assertTrue("Counter for 'WorkHistory' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testFindWorkHistory() {
        WorkHistory obj = dod.getRandomWorkHistory();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to provide an identifier", id);
        obj = workHistoryService.findWorkHistory(id);
        Assert.assertNotNull("Find method for 'WorkHistory' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'WorkHistory' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testFindAllWorkHistorys() {
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", dod.getRandomWorkHistory());
        long count = workHistoryService.countAllWorkHistorys();
        Assert.assertTrue("Too expensive to perform a find all test for 'WorkHistory', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<WorkHistory> result = workHistoryService.findAllWorkHistorys();
        Assert.assertNotNull("Find all method for 'WorkHistory' illegally returned null", result);
        Assert.assertTrue("Find all method for 'WorkHistory' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testFindWorkHistoryEntries() {
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", dod.getRandomWorkHistory());
        long count = workHistoryService.countAllWorkHistorys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<WorkHistory> result = workHistoryService.findWorkHistoryEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'WorkHistory' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'WorkHistory' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testFlush() {
        WorkHistory obj = dod.getRandomWorkHistory();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to provide an identifier", id);
        obj = workHistoryService.findWorkHistory(id);
        Assert.assertNotNull("Find method for 'WorkHistory' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyWorkHistory(obj);
        Integer currentVersion = obj.getVersion();
        workHistoryRepository.flush();
        Assert.assertTrue("Version for 'WorkHistory' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testUpdateWorkHistoryUpdate() {
        WorkHistory obj = dod.getRandomWorkHistory();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to provide an identifier", id);
        obj = workHistoryService.findWorkHistory(id);
        boolean modified =  dod.modifyWorkHistory(obj);
        Integer currentVersion = obj.getVersion();
        WorkHistory merged = workHistoryService.updateWorkHistory(obj);
        workHistoryRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'WorkHistory' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testSaveWorkHistory() {
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", dod.getRandomWorkHistory());
        WorkHistory obj = dod.getNewTransientWorkHistory(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'WorkHistory' identifier to be null", obj.getId());
        try {
            workHistoryService.saveWorkHistory(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        workHistoryRepository.flush();
        Assert.assertNotNull("Expected 'WorkHistory' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void WorkHistoryIntegrationTest.testDeleteWorkHistory() {
        WorkHistory obj = dod.getRandomWorkHistory();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'WorkHistory' failed to provide an identifier", id);
        obj = workHistoryService.findWorkHistory(id);
        workHistoryService.deleteWorkHistory(obj);
        workHistoryRepository.flush();
        Assert.assertNull("Failed to remove 'WorkHistory' with identifier '" + id + "'", workHistoryService.findWorkHistory(id));
    }
    
}
