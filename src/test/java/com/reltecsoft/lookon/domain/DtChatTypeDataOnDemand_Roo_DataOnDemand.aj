// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtChatType;
import com.reltecsoft.lookon.domain.DtChatTypeDataOnDemand;
import com.reltecsoft.lookon.repository.ChatTypeRepository;
import com.reltecsoft.lookon.service.ChatTypeService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect DtChatTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DtChatTypeDataOnDemand: @Component;
    
    private Random DtChatTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<DtChatType> DtChatTypeDataOnDemand.data;
    
    @Autowired
    ChatTypeService DtChatTypeDataOnDemand.chatTypeService;
    
    @Autowired
    ChatTypeRepository DtChatTypeDataOnDemand.chatTypeRepository;
    
    public DtChatType DtChatTypeDataOnDemand.getNewTransientDtChatType(int index) {
        DtChatType obj = new DtChatType();
        setTypeName(obj, index);
        return obj;
    }
    
    public void DtChatTypeDataOnDemand.setTypeName(DtChatType obj, int index) {
        String typeName = "typeName_" + index;
        if (typeName.length() > 50) {
            typeName = typeName.substring(0, 50);
        }
        obj.setTypeName(typeName);
    }
    
    public DtChatType DtChatTypeDataOnDemand.getSpecificDtChatType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        DtChatType obj = data.get(index);
        Integer id = obj.getId();
        return chatTypeService.findDtChatType(id);
    }
    
    public DtChatType DtChatTypeDataOnDemand.getRandomDtChatType() {
        init();
        DtChatType obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return chatTypeService.findDtChatType(id);
    }
    
    public boolean DtChatTypeDataOnDemand.modifyDtChatType(DtChatType obj) {
        return false;
    }
    
    public void DtChatTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = chatTypeService.findDtChatTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'DtChatType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<DtChatType>();
        for (int i = 0; i < 10; i++) {
            DtChatType obj = getNewTransientDtChatType(i);
            try {
                chatTypeService.saveDtChatType(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            chatTypeRepository.flush();
            data.add(obj);
        }
    }
    
}
