// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.PersonFriendDataOnDemand;
import com.reltecsoft.lookon.domain.PersonFriendIntegrationTest;
import com.reltecsoft.lookon.domain.PersonFriendPK;
import com.reltecsoft.lookon.repository.PersonFriendRepository;
import com.reltecsoft.lookon.service.PersonFriendService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersonFriendIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersonFriendIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersonFriendIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersonFriendIntegrationTest: @Transactional;
    
    @Autowired
    PersonFriendDataOnDemand PersonFriendIntegrationTest.dod;
    
    @Autowired
    PersonFriendService PersonFriendIntegrationTest.personFriendService;
    
    @Autowired
    PersonFriendRepository PersonFriendIntegrationTest.personFriendRepository;
    
    @Test
    public void PersonFriendIntegrationTest.testCountAllPersonFriends() {
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to initialize correctly", dod.getRandomPersonFriend());
        long count = personFriendService.countAllPersonFriends();
        Assert.assertTrue("Counter for 'PersonFriend' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersonFriendIntegrationTest.testFindPersonFriend() {
        PersonFriend obj = dod.getRandomPersonFriend();
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to initialize correctly", obj);
        PersonFriendPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to provide an identifier", id);
        obj = personFriendService.findPersonFriend(id);
        Assert.assertNotNull("Find method for 'PersonFriend' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PersonFriend' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersonFriendIntegrationTest.testFindAllPersonFriends() {
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to initialize correctly", dod.getRandomPersonFriend());
        long count = personFriendService.countAllPersonFriends();
        Assert.assertTrue("Too expensive to perform a find all test for 'PersonFriend', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PersonFriend> result = personFriendService.findAllPersonFriends();
        Assert.assertNotNull("Find all method for 'PersonFriend' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PersonFriend' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersonFriendIntegrationTest.testFindPersonFriendEntries() {
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to initialize correctly", dod.getRandomPersonFriend());
        long count = personFriendService.countAllPersonFriends();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PersonFriend> result = personFriendService.findPersonFriendEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PersonFriend' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PersonFriend' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersonFriendIntegrationTest.testSavePersonFriend() {
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to initialize correctly", dod.getRandomPersonFriend());
        PersonFriend obj = dod.getNewTransientPersonFriend(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to provide a new transient entity", obj);
        try {
            personFriendService.savePersonFriend(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        personFriendRepository.flush();
        Assert.assertNotNull("Expected 'PersonFriend' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersonFriendIntegrationTest.testDeletePersonFriend() {
        PersonFriend obj = dod.getRandomPersonFriend();
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to initialize correctly", obj);
        PersonFriendPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonFriend' failed to provide an identifier", id);
        obj = personFriendService.findPersonFriend(id);
        personFriendService.deletePersonFriend(obj);
        personFriendRepository.flush();
        Assert.assertNull("Failed to remove 'PersonFriend' with identifier '" + id + "'", personFriendService.findPersonFriend(id));
    }
    
}
