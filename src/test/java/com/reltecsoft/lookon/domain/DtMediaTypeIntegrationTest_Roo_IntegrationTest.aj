// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtMediaTypeDataOnDemand;
import com.reltecsoft.lookon.domain.DtMediaTypeIntegrationTest;
import com.reltecsoft.lookon.repository.MediaTypeRepository;
import com.reltecsoft.lookon.service.MediaTypeService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtMediaTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtMediaTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtMediaTypeIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtMediaTypeIntegrationTest: @Transactional;
    
    @Autowired
    DtMediaTypeDataOnDemand DtMediaTypeIntegrationTest.dod;
    
    @Autowired
    MediaTypeService DtMediaTypeIntegrationTest.mediaTypeService;
    
    @Autowired
    MediaTypeRepository DtMediaTypeIntegrationTest.mediaTypeRepository;
    
    @Test
    public void DtMediaTypeIntegrationTest.testCountAllDtMediaTypes() {
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to initialize correctly", dod.getRandomDtMediaType());
        long count = mediaTypeService.countAllDtMediaTypes();
        Assert.assertTrue("Counter for 'DtMediaType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtMediaTypeIntegrationTest.testFindDtMediaType() {
        DtMediaType obj = dod.getRandomDtMediaType();
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to provide an identifier", id);
        obj = mediaTypeService.findDtMediaType(id);
        Assert.assertNotNull("Find method for 'DtMediaType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtMediaType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtMediaTypeIntegrationTest.testFindAllDtMediaTypes() {
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to initialize correctly", dod.getRandomDtMediaType());
        long count = mediaTypeService.countAllDtMediaTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtMediaType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtMediaType> result = mediaTypeService.findAllDtMediaTypes();
        Assert.assertNotNull("Find all method for 'DtMediaType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtMediaType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtMediaTypeIntegrationTest.testFindDtMediaTypeEntries() {
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to initialize correctly", dod.getRandomDtMediaType());
        long count = mediaTypeService.countAllDtMediaTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtMediaType> result = mediaTypeService.findDtMediaTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtMediaType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtMediaType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtMediaTypeIntegrationTest.testSaveDtMediaType() {
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to initialize correctly", dod.getRandomDtMediaType());
        DtMediaType obj = dod.getNewTransientDtMediaType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtMediaType' identifier to be null", obj.getId());
        try {
            mediaTypeService.saveDtMediaType(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        mediaTypeRepository.flush();
        Assert.assertNotNull("Expected 'DtMediaType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtMediaTypeIntegrationTest.testDeleteDtMediaType() {
        DtMediaType obj = dod.getRandomDtMediaType();
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtMediaType' failed to provide an identifier", id);
        obj = mediaTypeService.findDtMediaType(id);
        mediaTypeService.deleteDtMediaType(obj);
        mediaTypeRepository.flush();
        Assert.assertNull("Failed to remove 'DtMediaType' with identifier '" + id + "'", mediaTypeService.findDtMediaType(id));
    }
    
}
