// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.ChatMessageDataOnDemand;
import com.reltecsoft.lookon.domain.ChatMessageIntegrationTest;
import com.reltecsoft.lookon.repository.ChatMessageRepository;
import com.reltecsoft.lookon.service.ChatMessageService;
import java.math.BigDecimal;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ChatMessageIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ChatMessageIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ChatMessageIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ChatMessageIntegrationTest: @Transactional;
    
    @Autowired
    ChatMessageDataOnDemand ChatMessageIntegrationTest.dod;
    
    @Autowired
    ChatMessageService ChatMessageIntegrationTest.chatMessageService;
    
    @Autowired
    ChatMessageRepository ChatMessageIntegrationTest.chatMessageRepository;
    
    @Test
    public void ChatMessageIntegrationTest.testCountAllChatMessages() {
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to initialize correctly", dod.getRandomChatMessage());
        long count = chatMessageService.countAllChatMessages();
        Assert.assertTrue("Counter for 'ChatMessage' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ChatMessageIntegrationTest.testFindChatMessage() {
        ChatMessage obj = dod.getRandomChatMessage();
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to initialize correctly", obj);
        BigDecimal id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to provide an identifier", id);
        obj = chatMessageService.findChatMessage(id);
        Assert.assertNotNull("Find method for 'ChatMessage' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ChatMessage' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ChatMessageIntegrationTest.testFindAllChatMessages() {
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to initialize correctly", dod.getRandomChatMessage());
        long count = chatMessageService.countAllChatMessages();
        Assert.assertTrue("Too expensive to perform a find all test for 'ChatMessage', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ChatMessage> result = chatMessageService.findAllChatMessages();
        Assert.assertNotNull("Find all method for 'ChatMessage' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ChatMessage' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ChatMessageIntegrationTest.testFindChatMessageEntries() {
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to initialize correctly", dod.getRandomChatMessage());
        long count = chatMessageService.countAllChatMessages();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ChatMessage> result = chatMessageService.findChatMessageEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ChatMessage' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ChatMessage' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ChatMessageIntegrationTest.testSaveChatMessage() {
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to initialize correctly", dod.getRandomChatMessage());
        ChatMessage obj = dod.getNewTransientChatMessage(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ChatMessage' identifier to be null", obj.getId());
        try {
            chatMessageService.saveChatMessage(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        chatMessageRepository.flush();
        Assert.assertNotNull("Expected 'ChatMessage' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ChatMessageIntegrationTest.testDeleteChatMessage() {
        ChatMessage obj = dod.getRandomChatMessage();
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to initialize correctly", obj);
        BigDecimal id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ChatMessage' failed to provide an identifier", id);
        obj = chatMessageService.findChatMessage(id);
        chatMessageService.deleteChatMessage(obj);
        chatMessageRepository.flush();
        Assert.assertNull("Failed to remove 'ChatMessage' with identifier '" + id + "'", chatMessageService.findChatMessage(id));
    }
    
}
