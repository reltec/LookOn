// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.ChatType;
import com.reltecsoft.lookon.domain.ChatTypeDataOnDemand;
import com.reltecsoft.lookon.repository.ChatTypeRepository;
import com.reltecsoft.lookon.service.ChatTypeService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect ChatTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: ChatTypeDataOnDemand: @Component;
    
    private Random ChatTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<ChatType> ChatTypeDataOnDemand.data;
    
    @Autowired
    ChatTypeService ChatTypeDataOnDemand.chatTypeService;
    
    @Autowired
    ChatTypeRepository ChatTypeDataOnDemand.chatTypeRepository;
    
    public ChatType ChatTypeDataOnDemand.getNewTransientChatType(int index) {
        ChatType obj = new ChatType();
        setTypeName(obj, index);
        return obj;
    }
    
    public void ChatTypeDataOnDemand.setTypeName(ChatType obj, int index) {
        String typeName = "typeName_" + index;
        if (typeName.length() > 50) {
            typeName = new Random().nextInt(10) + typeName.substring(1, 50);
        }
        obj.setTypeName(typeName);
    }
    
    public ChatType ChatTypeDataOnDemand.getSpecificChatType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        ChatType obj = data.get(index);
        Long id = obj.getId();
        return chatTypeService.findChatType(id);
    }
    
    public ChatType ChatTypeDataOnDemand.getRandomChatType() {
        init();
        ChatType obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return chatTypeService.findChatType(id);
    }
    
    public boolean ChatTypeDataOnDemand.modifyChatType(ChatType obj) {
        return false;
    }
    
    public void ChatTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = chatTypeService.findChatTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'ChatType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<ChatType>();
        for (int i = 0; i < 10; i++) {
            ChatType obj = getNewTransientChatType(i);
            try {
                chatTypeService.saveChatType(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            chatTypeRepository.flush();
            data.add(obj);
        }
    }
    
}
