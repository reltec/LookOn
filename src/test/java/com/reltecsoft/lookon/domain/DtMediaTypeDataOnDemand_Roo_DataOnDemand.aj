// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtMediaType;
import com.reltecsoft.lookon.domain.DtMediaTypeDataOnDemand;
import com.reltecsoft.lookon.repository.MediaTypeRepository;
import com.reltecsoft.lookon.service.MediaTypeService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect DtMediaTypeDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DtMediaTypeDataOnDemand: @Component;
    
    private Random DtMediaTypeDataOnDemand.rnd = new SecureRandom();
    
    private List<DtMediaType> DtMediaTypeDataOnDemand.data;
    
    @Autowired
    MediaTypeService DtMediaTypeDataOnDemand.mediaTypeService;
    
    @Autowired
    MediaTypeRepository DtMediaTypeDataOnDemand.mediaTypeRepository;
    
    public DtMediaType DtMediaTypeDataOnDemand.getNewTransientDtMediaType(int index) {
        DtMediaType obj = new DtMediaType();
        setTypeName(obj, index);
        return obj;
    }
    
    public void DtMediaTypeDataOnDemand.setTypeName(DtMediaType obj, int index) {
        String typeName = "typeName_" + index;
        if (typeName.length() > 20) {
            typeName = typeName.substring(0, 20);
        }
        obj.setTypeName(typeName);
    }
    
    public DtMediaType DtMediaTypeDataOnDemand.getSpecificDtMediaType(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        DtMediaType obj = data.get(index);
        Integer id = obj.getId();
        return mediaTypeService.findDtMediaType(id);
    }
    
    public DtMediaType DtMediaTypeDataOnDemand.getRandomDtMediaType() {
        init();
        DtMediaType obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return mediaTypeService.findDtMediaType(id);
    }
    
    public boolean DtMediaTypeDataOnDemand.modifyDtMediaType(DtMediaType obj) {
        return false;
    }
    
    public void DtMediaTypeDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = mediaTypeService.findDtMediaTypeEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'DtMediaType' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<DtMediaType>();
        for (int i = 0; i < 10; i++) {
            DtMediaType obj = getNewTransientDtMediaType(i);
            try {
                mediaTypeService.saveDtMediaType(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            mediaTypeRepository.flush();
            data.add(obj);
        }
    }
    
}
