// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.AlbumDataOnDemand;
import com.reltecsoft.lookon.domain.AlbumIntegrationTest;
import com.reltecsoft.lookon.repository.AlbumRepository;
import com.reltecsoft.lookon.service.AlbumService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect AlbumIntegrationTest_Roo_IntegrationTest {
    
    declare @type: AlbumIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: AlbumIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: AlbumIntegrationTest: @Transactional;
    
    @Autowired
    AlbumDataOnDemand AlbumIntegrationTest.dod;
    
    @Autowired
    AlbumService AlbumIntegrationTest.albumService;
    
    @Autowired
    AlbumRepository AlbumIntegrationTest.albumRepository;
    
    @Test
    public void AlbumIntegrationTest.testCountAllAlbums() {
        Assert.assertNotNull("Data on demand for 'Album' failed to initialize correctly", dod.getRandomAlbum());
        long count = albumService.countAllAlbums();
        Assert.assertTrue("Counter for 'Album' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void AlbumIntegrationTest.testFindAlbum() {
        Album obj = dod.getRandomAlbum();
        Assert.assertNotNull("Data on demand for 'Album' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Album' failed to provide an identifier", id);
        obj = albumService.findAlbum(id);
        Assert.assertNotNull("Find method for 'Album' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Album' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void AlbumIntegrationTest.testFindAllAlbums() {
        Assert.assertNotNull("Data on demand for 'Album' failed to initialize correctly", dod.getRandomAlbum());
        long count = albumService.countAllAlbums();
        Assert.assertTrue("Too expensive to perform a find all test for 'Album', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Album> result = albumService.findAllAlbums();
        Assert.assertNotNull("Find all method for 'Album' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Album' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void AlbumIntegrationTest.testFindAlbumEntries() {
        Assert.assertNotNull("Data on demand for 'Album' failed to initialize correctly", dod.getRandomAlbum());
        long count = albumService.countAllAlbums();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Album> result = albumService.findAlbumEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Album' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Album' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void AlbumIntegrationTest.testSaveAlbum() {
        Assert.assertNotNull("Data on demand for 'Album' failed to initialize correctly", dod.getRandomAlbum());
        Album obj = dod.getNewTransientAlbum(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Album' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Album' identifier to be null", obj.getId());
        try {
            albumService.saveAlbum(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        albumRepository.flush();
        Assert.assertNotNull("Expected 'Album' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void AlbumIntegrationTest.testDeleteAlbum() {
        Album obj = dod.getRandomAlbum();
        Assert.assertNotNull("Data on demand for 'Album' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Album' failed to provide an identifier", id);
        obj = albumService.findAlbum(id);
        albumService.deleteAlbum(obj);
        albumRepository.flush();
        Assert.assertNull("Failed to remove 'Album' with identifier '" + id + "'", albumService.findAlbum(id));
    }
    
}
