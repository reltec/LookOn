// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.SharedNotesDataOnDemand;
import com.reltecsoft.lookon.domain.SharedNotesIntegrationTest;
import com.reltecsoft.lookon.domain.SharedNotesPK;
import com.reltecsoft.lookon.repository.SharedNotesRepository;
import com.reltecsoft.lookon.service.SharedNotesService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect SharedNotesIntegrationTest_Roo_IntegrationTest {
    
    declare @type: SharedNotesIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: SharedNotesIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: SharedNotesIntegrationTest: @Transactional;
    
    @Autowired
    SharedNotesDataOnDemand SharedNotesIntegrationTest.dod;
    
    @Autowired
    SharedNotesService SharedNotesIntegrationTest.sharedNotesService;
    
    @Autowired
    SharedNotesRepository SharedNotesIntegrationTest.sharedNotesRepository;
    
    @Test
    public void SharedNotesIntegrationTest.testCountAllSharedNoteses() {
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to initialize correctly", dod.getRandomSharedNotes());
        long count = sharedNotesService.countAllSharedNoteses();
        Assert.assertTrue("Counter for 'SharedNotes' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void SharedNotesIntegrationTest.testFindSharedNotes() {
        SharedNotes obj = dod.getRandomSharedNotes();
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to initialize correctly", obj);
        SharedNotesPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to provide an identifier", id);
        obj = sharedNotesService.findSharedNotes(id);
        Assert.assertNotNull("Find method for 'SharedNotes' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'SharedNotes' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void SharedNotesIntegrationTest.testFindAllSharedNoteses() {
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to initialize correctly", dod.getRandomSharedNotes());
        long count = sharedNotesService.countAllSharedNoteses();
        Assert.assertTrue("Too expensive to perform a find all test for 'SharedNotes', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<SharedNotes> result = sharedNotesService.findAllSharedNoteses();
        Assert.assertNotNull("Find all method for 'SharedNotes' illegally returned null", result);
        Assert.assertTrue("Find all method for 'SharedNotes' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void SharedNotesIntegrationTest.testFindSharedNotesEntries() {
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to initialize correctly", dod.getRandomSharedNotes());
        long count = sharedNotesService.countAllSharedNoteses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<SharedNotes> result = sharedNotesService.findSharedNotesEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'SharedNotes' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'SharedNotes' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void SharedNotesIntegrationTest.testSaveSharedNotes() {
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to initialize correctly", dod.getRandomSharedNotes());
        SharedNotes obj = dod.getNewTransientSharedNotes(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to provide a new transient entity", obj);
        try {
            sharedNotesService.saveSharedNotes(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        sharedNotesRepository.flush();
        Assert.assertNotNull("Expected 'SharedNotes' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void SharedNotesIntegrationTest.testDeleteSharedNotes() {
        SharedNotes obj = dod.getRandomSharedNotes();
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to initialize correctly", obj);
        SharedNotesPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'SharedNotes' failed to provide an identifier", id);
        obj = sharedNotesService.findSharedNotes(id);
        sharedNotesService.deleteSharedNotes(obj);
        sharedNotesRepository.flush();
        Assert.assertNull("Failed to remove 'SharedNotes' with identifier '" + id + "'", sharedNotesService.findSharedNotes(id));
    }
    
}
