// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.MediaTypeDataOnDemand;
import com.reltecsoft.lookon.domain.MediaTypeIntegrationTest;
import com.reltecsoft.lookon.repository.MediaTypeRepository;
import com.reltecsoft.lookon.service.MediaTypeService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect MediaTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: MediaTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: MediaTypeIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: MediaTypeIntegrationTest: @Transactional;
    
    @Autowired
    MediaTypeDataOnDemand MediaTypeIntegrationTest.dod;
    
    @Autowired
    MediaTypeService MediaTypeIntegrationTest.mediaTypeService;
    
    @Autowired
    MediaTypeRepository MediaTypeIntegrationTest.mediaTypeRepository;
    
    @Test
    public void MediaTypeIntegrationTest.testCountAllMediaTypes() {
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", dod.getRandomMediaType());
        long count = mediaTypeService.countAllMediaTypes();
        Assert.assertTrue("Counter for 'MediaType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void MediaTypeIntegrationTest.testFindMediaType() {
        MediaType obj = dod.getRandomMediaType();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to provide an identifier", id);
        obj = mediaTypeService.findMediaType(id);
        Assert.assertNotNull("Find method for 'MediaType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'MediaType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void MediaTypeIntegrationTest.testFindAllMediaTypes() {
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", dod.getRandomMediaType());
        long count = mediaTypeService.countAllMediaTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'MediaType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<MediaType> result = mediaTypeService.findAllMediaTypes();
        Assert.assertNotNull("Find all method for 'MediaType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'MediaType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void MediaTypeIntegrationTest.testFindMediaTypeEntries() {
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", dod.getRandomMediaType());
        long count = mediaTypeService.countAllMediaTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<MediaType> result = mediaTypeService.findMediaTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'MediaType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'MediaType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void MediaTypeIntegrationTest.testFlush() {
        MediaType obj = dod.getRandomMediaType();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to provide an identifier", id);
        obj = mediaTypeService.findMediaType(id);
        Assert.assertNotNull("Find method for 'MediaType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyMediaType(obj);
        Integer currentVersion = obj.getVersion();
        mediaTypeRepository.flush();
        Assert.assertTrue("Version for 'MediaType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MediaTypeIntegrationTest.testUpdateMediaTypeUpdate() {
        MediaType obj = dod.getRandomMediaType();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to provide an identifier", id);
        obj = mediaTypeService.findMediaType(id);
        boolean modified =  dod.modifyMediaType(obj);
        Integer currentVersion = obj.getVersion();
        MediaType merged = mediaTypeService.updateMediaType(obj);
        mediaTypeRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'MediaType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void MediaTypeIntegrationTest.testSaveMediaType() {
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", dod.getRandomMediaType());
        MediaType obj = dod.getNewTransientMediaType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'MediaType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'MediaType' identifier to be null", obj.getId());
        try {
            mediaTypeService.saveMediaType(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        mediaTypeRepository.flush();
        Assert.assertNotNull("Expected 'MediaType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void MediaTypeIntegrationTest.testDeleteMediaType() {
        MediaType obj = dod.getRandomMediaType();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'MediaType' failed to provide an identifier", id);
        obj = mediaTypeService.findMediaType(id);
        mediaTypeService.deleteMediaType(obj);
        mediaTypeRepository.flush();
        Assert.assertNull("Failed to remove 'MediaType' with identifier '" + id + "'", mediaTypeService.findMediaType(id));
    }
    
}
