// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.ChatTypeDataOnDemand;
import com.reltecsoft.lookon.domain.ChatTypeIntegrationTest;
import com.reltecsoft.lookon.repository.ChatTypeRepository;
import com.reltecsoft.lookon.service.ChatTypeService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect ChatTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: ChatTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: ChatTypeIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: ChatTypeIntegrationTest: @Transactional;
    
    @Autowired
    ChatTypeDataOnDemand ChatTypeIntegrationTest.dod;
    
    @Autowired
    ChatTypeService ChatTypeIntegrationTest.chatTypeService;
    
    @Autowired
    ChatTypeRepository ChatTypeIntegrationTest.chatTypeRepository;
    
    @Test
    public void ChatTypeIntegrationTest.testCountAllChatTypes() {
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", dod.getRandomChatType());
        long count = chatTypeService.countAllChatTypes();
        Assert.assertTrue("Counter for 'ChatType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void ChatTypeIntegrationTest.testFindChatType() {
        ChatType obj = dod.getRandomChatType();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to provide an identifier", id);
        obj = chatTypeService.findChatType(id);
        Assert.assertNotNull("Find method for 'ChatType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'ChatType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void ChatTypeIntegrationTest.testFindAllChatTypes() {
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", dod.getRandomChatType());
        long count = chatTypeService.countAllChatTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'ChatType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<ChatType> result = chatTypeService.findAllChatTypes();
        Assert.assertNotNull("Find all method for 'ChatType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'ChatType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void ChatTypeIntegrationTest.testFindChatTypeEntries() {
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", dod.getRandomChatType());
        long count = chatTypeService.countAllChatTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<ChatType> result = chatTypeService.findChatTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'ChatType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'ChatType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void ChatTypeIntegrationTest.testFlush() {
        ChatType obj = dod.getRandomChatType();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to provide an identifier", id);
        obj = chatTypeService.findChatType(id);
        Assert.assertNotNull("Find method for 'ChatType' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyChatType(obj);
        Integer currentVersion = obj.getVersion();
        chatTypeRepository.flush();
        Assert.assertTrue("Version for 'ChatType' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ChatTypeIntegrationTest.testUpdateChatTypeUpdate() {
        ChatType obj = dod.getRandomChatType();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to provide an identifier", id);
        obj = chatTypeService.findChatType(id);
        boolean modified =  dod.modifyChatType(obj);
        Integer currentVersion = obj.getVersion();
        ChatType merged = chatTypeService.updateChatType(obj);
        chatTypeRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'ChatType' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void ChatTypeIntegrationTest.testSaveChatType() {
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", dod.getRandomChatType());
        ChatType obj = dod.getNewTransientChatType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'ChatType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'ChatType' identifier to be null", obj.getId());
        try {
            chatTypeService.saveChatType(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        chatTypeRepository.flush();
        Assert.assertNotNull("Expected 'ChatType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void ChatTypeIntegrationTest.testDeleteChatType() {
        ChatType obj = dod.getRandomChatType();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'ChatType' failed to provide an identifier", id);
        obj = chatTypeService.findChatType(id);
        chatTypeService.deleteChatType(obj);
        chatTypeRepository.flush();
        Assert.assertNull("Failed to remove 'ChatType' with identifier '" + id + "'", chatTypeService.findChatType(id));
    }
    
}
