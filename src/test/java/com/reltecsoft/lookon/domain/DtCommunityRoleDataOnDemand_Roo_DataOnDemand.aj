// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtCommunityRole;
import com.reltecsoft.lookon.domain.DtCommunityRoleDataOnDemand;
import com.reltecsoft.lookon.repository.CommunityRoleRepository;
import com.reltecsoft.lookon.service.CommunityRoleService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect DtCommunityRoleDataOnDemand_Roo_DataOnDemand {
    
    declare @type: DtCommunityRoleDataOnDemand: @Component;
    
    private Random DtCommunityRoleDataOnDemand.rnd = new SecureRandom();
    
    private List<DtCommunityRole> DtCommunityRoleDataOnDemand.data;
    
    @Autowired
    CommunityRoleService DtCommunityRoleDataOnDemand.communityRoleService;
    
    @Autowired
    CommunityRoleRepository DtCommunityRoleDataOnDemand.communityRoleRepository;
    
    public DtCommunityRole DtCommunityRoleDataOnDemand.getNewTransientDtCommunityRole(int index) {
        DtCommunityRole obj = new DtCommunityRole();
        setName(obj, index);
        return obj;
    }
    
    public void DtCommunityRoleDataOnDemand.setName(DtCommunityRole obj, int index) {
        String name = "name_" + index;
        if (name.length() > 50) {
            name = name.substring(0, 50);
        }
        obj.setName(name);
    }
    
    public DtCommunityRole DtCommunityRoleDataOnDemand.getSpecificDtCommunityRole(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        DtCommunityRole obj = data.get(index);
        Integer id = obj.getId();
        return communityRoleService.findDtCommunityRole(id);
    }
    
    public DtCommunityRole DtCommunityRoleDataOnDemand.getRandomDtCommunityRole() {
        init();
        DtCommunityRole obj = data.get(rnd.nextInt(data.size()));
        Integer id = obj.getId();
        return communityRoleService.findDtCommunityRole(id);
    }
    
    public boolean DtCommunityRoleDataOnDemand.modifyDtCommunityRole(DtCommunityRole obj) {
        return false;
    }
    
    public void DtCommunityRoleDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = communityRoleService.findDtCommunityRoleEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'DtCommunityRole' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<DtCommunityRole>();
        for (int i = 0; i < 10; i++) {
            DtCommunityRole obj = getNewTransientDtCommunityRole(i);
            try {
                communityRoleService.saveDtCommunityRole(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            communityRoleRepository.flush();
            data.add(obj);
        }
    }
    
}
