// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtNotesTypeDataOnDemand;
import com.reltecsoft.lookon.domain.DtNotesTypeIntegrationTest;
import com.reltecsoft.lookon.repository.NotesTypeRepository;
import com.reltecsoft.lookon.service.NotesTypeService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtNotesTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtNotesTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtNotesTypeIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtNotesTypeIntegrationTest: @Transactional;
    
    @Autowired
    DtNotesTypeDataOnDemand DtNotesTypeIntegrationTest.dod;
    
    @Autowired
    NotesTypeService DtNotesTypeIntegrationTest.notesTypeService;
    
    @Autowired
    NotesTypeRepository DtNotesTypeIntegrationTest.notesTypeRepository;
    
    @Test
    public void DtNotesTypeIntegrationTest.testCountAllDtNotesTypes() {
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to initialize correctly", dod.getRandomDtNotesType());
        long count = notesTypeService.countAllDtNotesTypes();
        Assert.assertTrue("Counter for 'DtNotesType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtNotesTypeIntegrationTest.testFindDtNotesType() {
        DtNotesType obj = dod.getRandomDtNotesType();
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to provide an identifier", id);
        obj = notesTypeService.findDtNotesType(id);
        Assert.assertNotNull("Find method for 'DtNotesType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtNotesType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtNotesTypeIntegrationTest.testFindAllDtNotesTypes() {
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to initialize correctly", dod.getRandomDtNotesType());
        long count = notesTypeService.countAllDtNotesTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtNotesType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtNotesType> result = notesTypeService.findAllDtNotesTypes();
        Assert.assertNotNull("Find all method for 'DtNotesType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtNotesType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtNotesTypeIntegrationTest.testFindDtNotesTypeEntries() {
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to initialize correctly", dod.getRandomDtNotesType());
        long count = notesTypeService.countAllDtNotesTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtNotesType> result = notesTypeService.findDtNotesTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtNotesType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtNotesType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtNotesTypeIntegrationTest.testSaveDtNotesType() {
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to initialize correctly", dod.getRandomDtNotesType());
        DtNotesType obj = dod.getNewTransientDtNotesType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtNotesType' identifier to be null", obj.getId());
        try {
            notesTypeService.saveDtNotesType(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        notesTypeRepository.flush();
        Assert.assertNotNull("Expected 'DtNotesType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtNotesTypeIntegrationTest.testDeleteDtNotesType() {
        DtNotesType obj = dod.getRandomDtNotesType();
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtNotesType' failed to provide an identifier", id);
        obj = notesTypeService.findDtNotesType(id);
        notesTypeService.deleteDtNotesType(obj);
        notesTypeRepository.flush();
        Assert.assertNull("Failed to remove 'DtNotesType' with identifier '" + id + "'", notesTypeService.findDtNotesType(id));
    }
    
}
