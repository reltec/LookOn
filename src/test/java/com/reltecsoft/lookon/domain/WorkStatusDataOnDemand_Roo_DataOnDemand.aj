// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.WorkStatus;
import com.reltecsoft.lookon.domain.WorkStatusDataOnDemand;
import com.reltecsoft.lookon.repository.WorkStatusRepository;
import com.reltecsoft.lookon.service.WorkStatusService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect WorkStatusDataOnDemand_Roo_DataOnDemand {
    
    declare @type: WorkStatusDataOnDemand: @Component;
    
    private Random WorkStatusDataOnDemand.rnd = new SecureRandom();
    
    private List<WorkStatus> WorkStatusDataOnDemand.data;
    
    @Autowired
    WorkStatusService WorkStatusDataOnDemand.workStatusService;
    
    @Autowired
    WorkStatusRepository WorkStatusDataOnDemand.workStatusRepository;
    
    public WorkStatus WorkStatusDataOnDemand.getNewTransientWorkStatus(int index) {
        WorkStatus obj = new WorkStatus();
        setName(obj, index);
        return obj;
    }
    
    public void WorkStatusDataOnDemand.setName(WorkStatus obj, int index) {
        String name = "name_" + index;
        if (name.length() > 20) {
            name = new Random().nextInt(10) + name.substring(1, 20);
        }
        obj.setName(name);
    }
    
    public WorkStatus WorkStatusDataOnDemand.getSpecificWorkStatus(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        WorkStatus obj = data.get(index);
        Long id = obj.getId();
        return workStatusService.findWorkStatus(id);
    }
    
    public WorkStatus WorkStatusDataOnDemand.getRandomWorkStatus() {
        init();
        WorkStatus obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return workStatusService.findWorkStatus(id);
    }
    
    public boolean WorkStatusDataOnDemand.modifyWorkStatus(WorkStatus obj) {
        return false;
    }
    
    public void WorkStatusDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = workStatusService.findWorkStatusEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'WorkStatus' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<WorkStatus>();
        for (int i = 0; i < 10; i++) {
            WorkStatus obj = getNewTransientWorkStatus(i);
            try {
                workStatusService.saveWorkStatus(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            workStatusRepository.flush();
            data.add(obj);
        }
    }
    
}
