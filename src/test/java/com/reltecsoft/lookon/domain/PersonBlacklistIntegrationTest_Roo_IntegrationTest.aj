// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.PersonBlacklistDataOnDemand;
import com.reltecsoft.lookon.domain.PersonBlacklistIntegrationTest;
import com.reltecsoft.lookon.domain.PersonBlacklistPK;
import com.reltecsoft.lookon.repository.PersonBlacklistRepository;
import com.reltecsoft.lookon.service.PersonBlacklistService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersonBlacklistIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersonBlacklistIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersonBlacklistIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersonBlacklistIntegrationTest: @Transactional;
    
    @Autowired
    PersonBlacklistDataOnDemand PersonBlacklistIntegrationTest.dod;
    
    @Autowired
    PersonBlacklistService PersonBlacklistIntegrationTest.personBlacklistService;
    
    @Autowired
    PersonBlacklistRepository PersonBlacklistIntegrationTest.personBlacklistRepository;
    
    @Test
    public void PersonBlacklistIntegrationTest.testCountAllPersonBlacklists() {
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to initialize correctly", dod.getRandomPersonBlacklist());
        long count = personBlacklistService.countAllPersonBlacklists();
        Assert.assertTrue("Counter for 'PersonBlacklist' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersonBlacklistIntegrationTest.testFindPersonBlacklist() {
        PersonBlacklist obj = dod.getRandomPersonBlacklist();
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to initialize correctly", obj);
        PersonBlacklistPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to provide an identifier", id);
        obj = personBlacklistService.findPersonBlacklist(id);
        Assert.assertNotNull("Find method for 'PersonBlacklist' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PersonBlacklist' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersonBlacklistIntegrationTest.testFindAllPersonBlacklists() {
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to initialize correctly", dod.getRandomPersonBlacklist());
        long count = personBlacklistService.countAllPersonBlacklists();
        Assert.assertTrue("Too expensive to perform a find all test for 'PersonBlacklist', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PersonBlacklist> result = personBlacklistService.findAllPersonBlacklists();
        Assert.assertNotNull("Find all method for 'PersonBlacklist' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PersonBlacklist' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersonBlacklistIntegrationTest.testFindPersonBlacklistEntries() {
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to initialize correctly", dod.getRandomPersonBlacklist());
        long count = personBlacklistService.countAllPersonBlacklists();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PersonBlacklist> result = personBlacklistService.findPersonBlacklistEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PersonBlacklist' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PersonBlacklist' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersonBlacklistIntegrationTest.testSavePersonBlacklist() {
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to initialize correctly", dod.getRandomPersonBlacklist());
        PersonBlacklist obj = dod.getNewTransientPersonBlacklist(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to provide a new transient entity", obj);
        try {
            personBlacklistService.savePersonBlacklist(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        personBlacklistRepository.flush();
        Assert.assertNotNull("Expected 'PersonBlacklist' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersonBlacklistIntegrationTest.testDeletePersonBlacklist() {
        PersonBlacklist obj = dod.getRandomPersonBlacklist();
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to initialize correctly", obj);
        PersonBlacklistPK id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonBlacklist' failed to provide an identifier", id);
        obj = personBlacklistService.findPersonBlacklist(id);
        personBlacklistService.deletePersonBlacklist(obj);
        personBlacklistRepository.flush();
        Assert.assertNull("Failed to remove 'PersonBlacklist' with identifier '" + id + "'", personBlacklistService.findPersonBlacklist(id));
    }
    
}
