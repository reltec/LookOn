// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtCompanyStatusDataOnDemand;
import com.reltecsoft.lookon.domain.DtCompanyStatusIntegrationTest;
import com.reltecsoft.lookon.repository.CompanyStatusRepository;
import com.reltecsoft.lookon.service.CompanyStatusService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtCompanyStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtCompanyStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtCompanyStatusIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtCompanyStatusIntegrationTest: @Transactional;
    
    @Autowired
    DtCompanyStatusDataOnDemand DtCompanyStatusIntegrationTest.dod;
    
    @Autowired
    CompanyStatusService DtCompanyStatusIntegrationTest.companyStatusService;
    
    @Autowired
    CompanyStatusRepository DtCompanyStatusIntegrationTest.companyStatusRepository;
    
    @Test
    public void DtCompanyStatusIntegrationTest.testCountAllDtCompanyStatuses() {
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to initialize correctly", dod.getRandomDtCompanyStatus());
        long count = companyStatusService.countAllDtCompanyStatuses();
        Assert.assertTrue("Counter for 'DtCompanyStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtCompanyStatusIntegrationTest.testFindDtCompanyStatus() {
        DtCompanyStatus obj = dod.getRandomDtCompanyStatus();
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to provide an identifier", id);
        obj = companyStatusService.findDtCompanyStatus(id);
        Assert.assertNotNull("Find method for 'DtCompanyStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtCompanyStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtCompanyStatusIntegrationTest.testFindAllDtCompanyStatuses() {
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to initialize correctly", dod.getRandomDtCompanyStatus());
        long count = companyStatusService.countAllDtCompanyStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtCompanyStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtCompanyStatus> result = companyStatusService.findAllDtCompanyStatuses();
        Assert.assertNotNull("Find all method for 'DtCompanyStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtCompanyStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtCompanyStatusIntegrationTest.testFindDtCompanyStatusEntries() {
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to initialize correctly", dod.getRandomDtCompanyStatus());
        long count = companyStatusService.countAllDtCompanyStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtCompanyStatus> result = companyStatusService.findDtCompanyStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtCompanyStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtCompanyStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtCompanyStatusIntegrationTest.testSaveDtCompanyStatus() {
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to initialize correctly", dod.getRandomDtCompanyStatus());
        DtCompanyStatus obj = dod.getNewTransientDtCompanyStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtCompanyStatus' identifier to be null", obj.getId());
        try {
            companyStatusService.saveDtCompanyStatus(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        companyStatusRepository.flush();
        Assert.assertNotNull("Expected 'DtCompanyStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtCompanyStatusIntegrationTest.testDeleteDtCompanyStatus() {
        DtCompanyStatus obj = dod.getRandomDtCompanyStatus();
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtCompanyStatus' failed to provide an identifier", id);
        obj = companyStatusService.findDtCompanyStatus(id);
        companyStatusService.deleteDtCompanyStatus(obj);
        companyStatusRepository.flush();
        Assert.assertNull("Failed to remove 'DtCompanyStatus' with identifier '" + id + "'", companyStatusService.findDtCompanyStatus(id));
    }
    
}
