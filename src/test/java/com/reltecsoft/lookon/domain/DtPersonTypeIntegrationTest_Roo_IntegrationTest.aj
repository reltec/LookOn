// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtPersonTypeDataOnDemand;
import com.reltecsoft.lookon.domain.DtPersonTypeIntegrationTest;
import com.reltecsoft.lookon.repository.PersonTypeRepository;
import com.reltecsoft.lookon.service.PersonTypeService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtPersonTypeIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtPersonTypeIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtPersonTypeIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtPersonTypeIntegrationTest: @Transactional;
    
    @Autowired
    DtPersonTypeDataOnDemand DtPersonTypeIntegrationTest.dod;
    
    @Autowired
    PersonTypeService DtPersonTypeIntegrationTest.personTypeService;
    
    @Autowired
    PersonTypeRepository DtPersonTypeIntegrationTest.personTypeRepository;
    
    @Test
    public void DtPersonTypeIntegrationTest.testCountAllDtPersonTypes() {
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to initialize correctly", dod.getRandomDtPersonType());
        long count = personTypeService.countAllDtPersonTypes();
        Assert.assertTrue("Counter for 'DtPersonType' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtPersonTypeIntegrationTest.testFindDtPersonType() {
        DtPersonType obj = dod.getRandomDtPersonType();
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to provide an identifier", id);
        obj = personTypeService.findDtPersonType(id);
        Assert.assertNotNull("Find method for 'DtPersonType' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtPersonType' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtPersonTypeIntegrationTest.testFindAllDtPersonTypes() {
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to initialize correctly", dod.getRandomDtPersonType());
        long count = personTypeService.countAllDtPersonTypes();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtPersonType', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtPersonType> result = personTypeService.findAllDtPersonTypes();
        Assert.assertNotNull("Find all method for 'DtPersonType' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtPersonType' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtPersonTypeIntegrationTest.testFindDtPersonTypeEntries() {
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to initialize correctly", dod.getRandomDtPersonType());
        long count = personTypeService.countAllDtPersonTypes();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtPersonType> result = personTypeService.findDtPersonTypeEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtPersonType' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtPersonType' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtPersonTypeIntegrationTest.testSaveDtPersonType() {
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to initialize correctly", dod.getRandomDtPersonType());
        DtPersonType obj = dod.getNewTransientDtPersonType(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtPersonType' identifier to be null", obj.getId());
        try {
            personTypeService.saveDtPersonType(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        personTypeRepository.flush();
        Assert.assertNotNull("Expected 'DtPersonType' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtPersonTypeIntegrationTest.testDeleteDtPersonType() {
        DtPersonType obj = dod.getRandomDtPersonType();
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtPersonType' failed to provide an identifier", id);
        obj = personTypeService.findDtPersonType(id);
        personTypeService.deleteDtPersonType(obj);
        personTypeRepository.flush();
        Assert.assertNull("Failed to remove 'DtPersonType' with identifier '" + id + "'", personTypeService.findDtPersonType(id));
    }
    
}
