// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtPersonActivityStatusDataOnDemand;
import com.reltecsoft.lookon.domain.DtPersonActivityStatusIntegrationTest;
import com.reltecsoft.lookon.repository.PersonActivityStatusRepository;
import com.reltecsoft.lookon.service.PersonActivityStatusService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtPersonActivityStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtPersonActivityStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtPersonActivityStatusIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtPersonActivityStatusIntegrationTest: @Transactional;
    
    @Autowired
    DtPersonActivityStatusDataOnDemand DtPersonActivityStatusIntegrationTest.dod;
    
    @Autowired
    PersonActivityStatusService DtPersonActivityStatusIntegrationTest.personActivityStatusService;
    
    @Autowired
    PersonActivityStatusRepository DtPersonActivityStatusIntegrationTest.personActivityStatusRepository;
    
    @Test
    public void DtPersonActivityStatusIntegrationTest.testCountAllDtPersonActivityStatuses() {
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to initialize correctly", dod.getRandomDtPersonActivityStatus());
        long count = personActivityStatusService.countAllDtPersonActivityStatuses();
        Assert.assertTrue("Counter for 'DtPersonActivityStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtPersonActivityStatusIntegrationTest.testFindDtPersonActivityStatus() {
        DtPersonActivityStatus obj = dod.getRandomDtPersonActivityStatus();
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to provide an identifier", id);
        obj = personActivityStatusService.findDtPersonActivityStatus(id);
        Assert.assertNotNull("Find method for 'DtPersonActivityStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtPersonActivityStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtPersonActivityStatusIntegrationTest.testFindAllDtPersonActivityStatuses() {
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to initialize correctly", dod.getRandomDtPersonActivityStatus());
        long count = personActivityStatusService.countAllDtPersonActivityStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtPersonActivityStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtPersonActivityStatus> result = personActivityStatusService.findAllDtPersonActivityStatuses();
        Assert.assertNotNull("Find all method for 'DtPersonActivityStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtPersonActivityStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtPersonActivityStatusIntegrationTest.testFindDtPersonActivityStatusEntries() {
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to initialize correctly", dod.getRandomDtPersonActivityStatus());
        long count = personActivityStatusService.countAllDtPersonActivityStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtPersonActivityStatus> result = personActivityStatusService.findDtPersonActivityStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtPersonActivityStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtPersonActivityStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtPersonActivityStatusIntegrationTest.testSaveDtPersonActivityStatus() {
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to initialize correctly", dod.getRandomDtPersonActivityStatus());
        DtPersonActivityStatus obj = dod.getNewTransientDtPersonActivityStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtPersonActivityStatus' identifier to be null", obj.getId());
        try {
            personActivityStatusService.saveDtPersonActivityStatus(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        personActivityStatusRepository.flush();
        Assert.assertNotNull("Expected 'DtPersonActivityStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtPersonActivityStatusIntegrationTest.testDeleteDtPersonActivityStatus() {
        DtPersonActivityStatus obj = dod.getRandomDtPersonActivityStatus();
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtPersonActivityStatus' failed to provide an identifier", id);
        obj = personActivityStatusService.findDtPersonActivityStatus(id);
        personActivityStatusService.deleteDtPersonActivityStatus(obj);
        personActivityStatusRepository.flush();
        Assert.assertNull("Failed to remove 'DtPersonActivityStatus' with identifier '" + id + "'", personActivityStatusService.findDtPersonActivityStatus(id));
    }
    
}
