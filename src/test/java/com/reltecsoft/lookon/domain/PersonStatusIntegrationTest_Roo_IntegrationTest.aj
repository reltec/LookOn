// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.PersonStatusDataOnDemand;
import com.reltecsoft.lookon.domain.PersonStatusIntegrationTest;
import com.reltecsoft.lookon.repository.PersonStatusRepository;
import com.reltecsoft.lookon.service.PersonStatusService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect PersonStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: PersonStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: PersonStatusIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: PersonStatusIntegrationTest: @Transactional;
    
    @Autowired
    PersonStatusDataOnDemand PersonStatusIntegrationTest.dod;
    
    @Autowired
    PersonStatusService PersonStatusIntegrationTest.personStatusService;
    
    @Autowired
    PersonStatusRepository PersonStatusIntegrationTest.personStatusRepository;
    
    @Test
    public void PersonStatusIntegrationTest.testCountAllPersonStatuses() {
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", dod.getRandomPersonStatus());
        long count = personStatusService.countAllPersonStatuses();
        Assert.assertTrue("Counter for 'PersonStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void PersonStatusIntegrationTest.testFindPersonStatus() {
        PersonStatus obj = dod.getRandomPersonStatus();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to provide an identifier", id);
        obj = personStatusService.findPersonStatus(id);
        Assert.assertNotNull("Find method for 'PersonStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'PersonStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void PersonStatusIntegrationTest.testFindAllPersonStatuses() {
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", dod.getRandomPersonStatus());
        long count = personStatusService.countAllPersonStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'PersonStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<PersonStatus> result = personStatusService.findAllPersonStatuses();
        Assert.assertNotNull("Find all method for 'PersonStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'PersonStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void PersonStatusIntegrationTest.testFindPersonStatusEntries() {
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", dod.getRandomPersonStatus());
        long count = personStatusService.countAllPersonStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<PersonStatus> result = personStatusService.findPersonStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'PersonStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'PersonStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void PersonStatusIntegrationTest.testFlush() {
        PersonStatus obj = dod.getRandomPersonStatus();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to provide an identifier", id);
        obj = personStatusService.findPersonStatus(id);
        Assert.assertNotNull("Find method for 'PersonStatus' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyPersonStatus(obj);
        Integer currentVersion = obj.getVersion();
        personStatusRepository.flush();
        Assert.assertTrue("Version for 'PersonStatus' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PersonStatusIntegrationTest.testUpdatePersonStatusUpdate() {
        PersonStatus obj = dod.getRandomPersonStatus();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to provide an identifier", id);
        obj = personStatusService.findPersonStatus(id);
        boolean modified =  dod.modifyPersonStatus(obj);
        Integer currentVersion = obj.getVersion();
        PersonStatus merged = personStatusService.updatePersonStatus(obj);
        personStatusRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'PersonStatus' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void PersonStatusIntegrationTest.testSavePersonStatus() {
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", dod.getRandomPersonStatus());
        PersonStatus obj = dod.getNewTransientPersonStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'PersonStatus' identifier to be null", obj.getId());
        try {
            personStatusService.savePersonStatus(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        personStatusRepository.flush();
        Assert.assertNotNull("Expected 'PersonStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void PersonStatusIntegrationTest.testDeletePersonStatus() {
        PersonStatus obj = dod.getRandomPersonStatus();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'PersonStatus' failed to provide an identifier", id);
        obj = personStatusService.findPersonStatus(id);
        personStatusService.deletePersonStatus(obj);
        personStatusRepository.flush();
        Assert.assertNull("Failed to remove 'PersonStatus' with identifier '" + id + "'", personStatusService.findPersonStatus(id));
    }
    
}
