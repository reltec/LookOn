// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.MediaDataOnDemand;
import com.reltecsoft.lookon.domain.PersonDataOnDemand;
import com.reltecsoft.lookon.domain.Workspace;
import com.reltecsoft.lookon.domain.WorkspaceDataOnDemand;
import com.reltecsoft.lookon.repository.WorkspaceRepository;
import com.reltecsoft.lookon.service.WorkspaceService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect WorkspaceDataOnDemand_Roo_DataOnDemand {
    
    declare @type: WorkspaceDataOnDemand: @Component;
    
    private Random WorkspaceDataOnDemand.rnd = new SecureRandom();
    
    private List<Workspace> WorkspaceDataOnDemand.data;
    
    @Autowired
    PersonDataOnDemand WorkspaceDataOnDemand.personDataOnDemand;
    
    @Autowired
    MediaDataOnDemand WorkspaceDataOnDemand.mediaDataOnDemand;
    
    @Autowired
    WorkspaceService WorkspaceDataOnDemand.workspaceService;
    
    @Autowired
    WorkspaceRepository WorkspaceDataOnDemand.workspaceRepository;
    
    public Workspace WorkspaceDataOnDemand.getNewTransientWorkspace(int index) {
        Workspace obj = new Workspace();
        setPerson(obj, index);
        return obj;
    }
    
    public Workspace WorkspaceDataOnDemand.getSpecificWorkspace(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        Workspace obj = data.get(index);
        Long id = obj.getId();
        return workspaceService.findWorkspace(id);
    }
    
    public Workspace WorkspaceDataOnDemand.getRandomWorkspace() {
        init();
        Workspace obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return workspaceService.findWorkspace(id);
    }
    
    public boolean WorkspaceDataOnDemand.modifyWorkspace(Workspace obj) {
        return false;
    }
    
    public void WorkspaceDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = workspaceService.findWorkspaceEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'Workspace' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<Workspace>();
        for (int i = 0; i < 10; i++) {
            Workspace obj = getNewTransientWorkspace(i);
            try {
                workspaceService.saveWorkspace(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            workspaceRepository.flush();
            data.add(obj);
        }
    }
    
}
