// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtVacancyStatusDataOnDemand;
import com.reltecsoft.lookon.domain.DtVacancyStatusIntegrationTest;
import com.reltecsoft.lookon.repository.VacancyStatusRepository;
import com.reltecsoft.lookon.service.VacancyStatusService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtVacancyStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtVacancyStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtVacancyStatusIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtVacancyStatusIntegrationTest: @Transactional;
    
    @Autowired
    DtVacancyStatusDataOnDemand DtVacancyStatusIntegrationTest.dod;
    
    @Autowired
    VacancyStatusService DtVacancyStatusIntegrationTest.vacancyStatusService;
    
    @Autowired
    VacancyStatusRepository DtVacancyStatusIntegrationTest.vacancyStatusRepository;
    
    @Test
    public void DtVacancyStatusIntegrationTest.testCountAllDtVacancyStatuses() {
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to initialize correctly", dod.getRandomDtVacancyStatus());
        long count = vacancyStatusService.countAllDtVacancyStatuses();
        Assert.assertTrue("Counter for 'DtVacancyStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtVacancyStatusIntegrationTest.testFindDtVacancyStatus() {
        DtVacancyStatus obj = dod.getRandomDtVacancyStatus();
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to provide an identifier", id);
        obj = vacancyStatusService.findDtVacancyStatus(id);
        Assert.assertNotNull("Find method for 'DtVacancyStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtVacancyStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtVacancyStatusIntegrationTest.testFindAllDtVacancyStatuses() {
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to initialize correctly", dod.getRandomDtVacancyStatus());
        long count = vacancyStatusService.countAllDtVacancyStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtVacancyStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtVacancyStatus> result = vacancyStatusService.findAllDtVacancyStatuses();
        Assert.assertNotNull("Find all method for 'DtVacancyStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtVacancyStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtVacancyStatusIntegrationTest.testFindDtVacancyStatusEntries() {
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to initialize correctly", dod.getRandomDtVacancyStatus());
        long count = vacancyStatusService.countAllDtVacancyStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtVacancyStatus> result = vacancyStatusService.findDtVacancyStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtVacancyStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtVacancyStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtVacancyStatusIntegrationTest.testSaveDtVacancyStatus() {
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to initialize correctly", dod.getRandomDtVacancyStatus());
        DtVacancyStatus obj = dod.getNewTransientDtVacancyStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtVacancyStatus' identifier to be null", obj.getId());
        try {
            vacancyStatusService.saveDtVacancyStatus(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        vacancyStatusRepository.flush();
        Assert.assertNotNull("Expected 'DtVacancyStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtVacancyStatusIntegrationTest.testDeleteDtVacancyStatus() {
        DtVacancyStatus obj = dod.getRandomDtVacancyStatus();
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtVacancyStatus' failed to provide an identifier", id);
        obj = vacancyStatusService.findDtVacancyStatus(id);
        vacancyStatusService.deleteDtVacancyStatus(obj);
        vacancyStatusRepository.flush();
        Assert.assertNull("Failed to remove 'DtVacancyStatus' with identifier '" + id + "'", vacancyStatusService.findDtVacancyStatus(id));
    }
    
}
