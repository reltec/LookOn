// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.VacancyDataOnDemand;
import com.reltecsoft.lookon.domain.VacancyIntegrationTest;
import com.reltecsoft.lookon.repository.VacancyRepository;
import com.reltecsoft.lookon.service.VacancyService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect VacancyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: VacancyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: VacancyIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: VacancyIntegrationTest: @Transactional;
    
    @Autowired
    VacancyDataOnDemand VacancyIntegrationTest.dod;
    
    @Autowired
    VacancyService VacancyIntegrationTest.vacancyService;
    
    @Autowired
    VacancyRepository VacancyIntegrationTest.vacancyRepository;
    
    @Test
    public void VacancyIntegrationTest.testCountAllVacancys() {
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to initialize correctly", dod.getRandomVacancy());
        long count = vacancyService.countAllVacancys();
        Assert.assertTrue("Counter for 'Vacancy' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void VacancyIntegrationTest.testFindVacancy() {
        Vacancy obj = dod.getRandomVacancy();
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to provide an identifier", id);
        obj = vacancyService.findVacancy(id);
        Assert.assertNotNull("Find method for 'Vacancy' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'Vacancy' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void VacancyIntegrationTest.testFindAllVacancys() {
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to initialize correctly", dod.getRandomVacancy());
        long count = vacancyService.countAllVacancys();
        Assert.assertTrue("Too expensive to perform a find all test for 'Vacancy', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<Vacancy> result = vacancyService.findAllVacancys();
        Assert.assertNotNull("Find all method for 'Vacancy' illegally returned null", result);
        Assert.assertTrue("Find all method for 'Vacancy' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void VacancyIntegrationTest.testFindVacancyEntries() {
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to initialize correctly", dod.getRandomVacancy());
        long count = vacancyService.countAllVacancys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<Vacancy> result = vacancyService.findVacancyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'Vacancy' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'Vacancy' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void VacancyIntegrationTest.testSaveVacancy() {
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to initialize correctly", dod.getRandomVacancy());
        Vacancy obj = dod.getNewTransientVacancy(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'Vacancy' identifier to be null", obj.getId());
        try {
            vacancyService.saveVacancy(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        vacancyRepository.flush();
        Assert.assertNotNull("Expected 'Vacancy' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void VacancyIntegrationTest.testDeleteVacancy() {
        Vacancy obj = dod.getRandomVacancy();
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'Vacancy' failed to provide an identifier", id);
        obj = vacancyService.findVacancy(id);
        vacancyService.deleteVacancy(obj);
        vacancyRepository.flush();
        Assert.assertNull("Failed to remove 'Vacancy' with identifier '" + id + "'", vacancyService.findVacancy(id));
    }
    
}
