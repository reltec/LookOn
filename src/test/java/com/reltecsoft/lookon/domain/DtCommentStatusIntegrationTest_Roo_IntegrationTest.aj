// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.DtCommentStatusDataOnDemand;
import com.reltecsoft.lookon.domain.DtCommentStatusIntegrationTest;
import com.reltecsoft.lookon.repository.CommentStatusRepository;
import com.reltecsoft.lookon.service.CommentStatusService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect DtCommentStatusIntegrationTest_Roo_IntegrationTest {
    
    declare @type: DtCommentStatusIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: DtCommentStatusIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: DtCommentStatusIntegrationTest: @Transactional;
    
    @Autowired
    DtCommentStatusDataOnDemand DtCommentStatusIntegrationTest.dod;
    
    @Autowired
    CommentStatusService DtCommentStatusIntegrationTest.commentStatusService;
    
    @Autowired
    CommentStatusRepository DtCommentStatusIntegrationTest.commentStatusRepository;
    
    @Test
    public void DtCommentStatusIntegrationTest.testCountAllDtCommentStatuses() {
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to initialize correctly", dod.getRandomDtCommentStatus());
        long count = commentStatusService.countAllDtCommentStatuses();
        Assert.assertTrue("Counter for 'DtCommentStatus' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void DtCommentStatusIntegrationTest.testFindDtCommentStatus() {
        DtCommentStatus obj = dod.getRandomDtCommentStatus();
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to provide an identifier", id);
        obj = commentStatusService.findDtCommentStatus(id);
        Assert.assertNotNull("Find method for 'DtCommentStatus' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'DtCommentStatus' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void DtCommentStatusIntegrationTest.testFindAllDtCommentStatuses() {
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to initialize correctly", dod.getRandomDtCommentStatus());
        long count = commentStatusService.countAllDtCommentStatuses();
        Assert.assertTrue("Too expensive to perform a find all test for 'DtCommentStatus', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<DtCommentStatus> result = commentStatusService.findAllDtCommentStatuses();
        Assert.assertNotNull("Find all method for 'DtCommentStatus' illegally returned null", result);
        Assert.assertTrue("Find all method for 'DtCommentStatus' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void DtCommentStatusIntegrationTest.testFindDtCommentStatusEntries() {
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to initialize correctly", dod.getRandomDtCommentStatus());
        long count = commentStatusService.countAllDtCommentStatuses();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<DtCommentStatus> result = commentStatusService.findDtCommentStatusEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'DtCommentStatus' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'DtCommentStatus' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void DtCommentStatusIntegrationTest.testSaveDtCommentStatus() {
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to initialize correctly", dod.getRandomDtCommentStatus());
        DtCommentStatus obj = dod.getNewTransientDtCommentStatus(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'DtCommentStatus' identifier to be null", obj.getId());
        try {
            commentStatusService.saveDtCommentStatus(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        commentStatusRepository.flush();
        Assert.assertNotNull("Expected 'DtCommentStatus' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void DtCommentStatusIntegrationTest.testDeleteDtCommentStatus() {
        DtCommentStatus obj = dod.getRandomDtCommentStatus();
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to initialize correctly", obj);
        Integer id = obj.getId();
        Assert.assertNotNull("Data on demand for 'DtCommentStatus' failed to provide an identifier", id);
        obj = commentStatusService.findDtCommentStatus(id);
        commentStatusService.deleteDtCommentStatus(obj);
        commentStatusRepository.flush();
        Assert.assertNull("Failed to remove 'DtCommentStatus' with identifier '" + id + "'", commentStatusService.findDtCommentStatus(id));
    }
    
}
