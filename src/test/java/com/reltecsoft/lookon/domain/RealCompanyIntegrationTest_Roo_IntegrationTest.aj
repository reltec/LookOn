// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.reltecsoft.lookon.domain;

import com.reltecsoft.lookon.domain.RealCompanyDataOnDemand;
import com.reltecsoft.lookon.domain.RealCompanyIntegrationTest;
import com.reltecsoft.lookon.repository.RealCompanyRepository;
import com.reltecsoft.lookon.service.RealCompanyService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect RealCompanyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: RealCompanyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: RealCompanyIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: RealCompanyIntegrationTest: @Transactional;
    
    @Autowired
    RealCompanyDataOnDemand RealCompanyIntegrationTest.dod;
    
    @Autowired
    RealCompanyService RealCompanyIntegrationTest.realCompanyService;
    
    @Autowired
    RealCompanyRepository RealCompanyIntegrationTest.realCompanyRepository;
    
    @Test
    public void RealCompanyIntegrationTest.testCountAllRealCompanys() {
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to initialize correctly", dod.getRandomRealCompany());
        long count = realCompanyService.countAllRealCompanys();
        Assert.assertTrue("Counter for 'RealCompany' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void RealCompanyIntegrationTest.testFindRealCompany() {
        RealCompany obj = dod.getRandomRealCompany();
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to provide an identifier", id);
        obj = realCompanyService.findRealCompany(id);
        Assert.assertNotNull("Find method for 'RealCompany' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'RealCompany' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void RealCompanyIntegrationTest.testFindAllRealCompanys() {
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to initialize correctly", dod.getRandomRealCompany());
        long count = realCompanyService.countAllRealCompanys();
        Assert.assertTrue("Too expensive to perform a find all test for 'RealCompany', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<RealCompany> result = realCompanyService.findAllRealCompanys();
        Assert.assertNotNull("Find all method for 'RealCompany' illegally returned null", result);
        Assert.assertTrue("Find all method for 'RealCompany' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void RealCompanyIntegrationTest.testFindRealCompanyEntries() {
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to initialize correctly", dod.getRandomRealCompany());
        long count = realCompanyService.countAllRealCompanys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<RealCompany> result = realCompanyService.findRealCompanyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'RealCompany' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'RealCompany' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void RealCompanyIntegrationTest.testSaveRealCompany() {
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to initialize correctly", dod.getRandomRealCompany());
        RealCompany obj = dod.getNewTransientRealCompany(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'RealCompany' identifier to be null", obj.getId());
        try {
            realCompanyService.saveRealCompany(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        realCompanyRepository.flush();
        Assert.assertNotNull("Expected 'RealCompany' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void RealCompanyIntegrationTest.testDeleteRealCompany() {
        RealCompany obj = dod.getRandomRealCompany();
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'RealCompany' failed to provide an identifier", id);
        obj = realCompanyService.findRealCompany(id);
        realCompanyService.deleteRealCompany(obj);
        realCompanyRepository.flush();
        Assert.assertNull("Failed to remove 'RealCompany' with identifier '" + id + "'", realCompanyService.findRealCompany(id));
    }
    
}
